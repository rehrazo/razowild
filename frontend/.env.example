# Optional: frontend admin token used for protected admin write requests
# Must match backend ADMIN_API_TOKEN when token-based admin auth is used
# Not required when using admin JWT login flow
VITE_ADMIN_API_TOKEN=replace_with_the_same_admin_token
